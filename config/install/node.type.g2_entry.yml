langcode: en
status: true
dependencies:
  enforced:
    module:
      - g2
name: 'G2 Entry'
type: g2_entry
description: 'A G2 entry is a term (usual sense, not Drupal sense) for which a definition and various additional information is provided, notably at the editorial level. It includes built-in fields for glossary logic. Only use for G2 glossaries.'
help: ''
new_revision: true
preview_mode: 1
display_submitted: false
