$Id$
$Name$

2006-02-04
  FEAT  Use a vocabulary to disambiguate entries on the homonyms page
        The vocabulary is chosen on admin/settings/g2
2006-02-02
  FEAT  New feature: list of recently updated entries in user profile
  SECU  Security fix: IP-based access control was not implemented
2006-01-28
  FEAT  New feature: referrer tracking. Adds a tab on the node edit form, 
        a setting and a path to wipe referers for a node or for all g2 nodes
2006-01-27
  FIX   This version is now compatible with Drupal 4.7beta4: this required a change in g2_view
  FEAT  Themeability improvement: blocks now use a <div class="content">
2006-01-16
  FEAT  The URL of the main page is now a module setting, grouped with its nid
  FEAT  RPC setting and version info have been moved to a collapsed fieldset
        to mask them from first sight
  FEAT  Under the default themeing, when a link is done to an "entries" page
        matching no definition, a link to automatically create one is provided
        if user has g2 admin permission.
  FIX   "More" links ('+') now carry the standard "read-more" class
        for standard themes
2006-01-15
  FIX   PHP code in g2 main page now works with default themes 
        as in http://drupal.org/node/44756
  FEAT  'By initial' page now displays stats http://drupal.org/node/44800
  QUAL  Debugging watchog removed
  QUAL  NODE constants introduced for core version missing them
2006-01-14
  FIX   Fix for unpublished nodes being returned http://drupal.org/node/44722 and
  FEAT  New g2.stats RPC function
2006-01-13
  FIX   Themable functions are now used correctly within the module itself
  FIX   Added wrappers around all theme functions for themeability
2006-01-11
  FIX   Taxonomy terms are no longer displayed or returned by WOTD for 
        freetagging vocabularies, unless admin so chooses. They are only 
        available to g2 node editors.
  FIX   Security: RPC is now disabled by default.
  FEAT  A new setting has been added to allow site admins to relax the
        "entries must include a definition" requirement
  FEAT  Definition teasers including html code are now stripped instead of
        being converted to plain text
2006-01-02
  FIX   Title for the "entries by initial" page now includes the initial
        for http://drupal.org/node/43479
  FEAT  Override/extend the author name for entries when displayed in the
        WOTD RSS feed
2006-01-01
  FIX   Items in the WOTD RSS feed are now dated with the day they are
        requested instead of the node creation date.
  FIX   G2-generated links in the RSS feed (and on node pages) are now
        absolute: feed now validates without warnings.
  FEAT  Setting for the description of the WOTD RSS feed
  FEAT  Setting for the title of the WOTD RSS feed, now distinct from the
        title of the WOTD block, but defaulting to it if nulled
  FEAT  RSS feed for word of the day http://drupal.org/node/43078
  FEAT  homonyms disambiguation page at “<drupal>/g2/entries/<someterm>” is
        now customizable at the site level, and themeable, and can thus be
        overriden both by site webmasters and by themes
        for http://drupal.org/node/42965
  FEAT  Main page at <drupal>/g2 is now themeable, and can thus be
        overriden by themes (it could already be overriden by site webmasters)
  FIX   Added G2 path in content/add
  FIX   WOTD autochange now works
  FIX   Fixed constant name errors in g2_settings: constants in that form
        were not duly prefixed.
  FEAT  Setting to enable auto-jump from disambiguation on single entry
        match for http://drupal.org/node/42947
  FEAT  Setting to toggle display of taxonomy terms in random and WOTD blocks
        for http://drupal.org/node/42928
2005-12-31
  FEAT  Patch to optimize random block on high-volume sites
        for http://drupal.org/node/42842
2005-12-30
  FIX   Patches for Naudefj http://drupal.org/node/42750
  QUAL  Code cleanup: many strings moved to one of the constant groups on top
        of the file for easier maintenability, notably all of the persistent
        variable names http://drupal.org/node/42750
  FEAT  Setting to optimize search on entry titles

2005-12-29
        Version 1.0 Beta 1 (on the G2 wiki)
  FIX   Page by initial segment now works (in other words,
        the alphabar points to valid pages)
2005-12-28
  FEAT  All four predefined blocks are now fully themeable.
  FEAT  The initial themeing functions theme_g2_wotd_teaser and
        theme_g2_wotd_body have been replaced by theme_g2_block_wotd
2005-12-24
  FIX   Bug fix: http: //drupal.org/node/42247 G2 no longer links to
        unpublished nodes.

2005-12-23 v 1.1
        Initial CVS files by Frederic G. MARAND (fgm@osinet.fr).
        Commit by Chris. Thanks to him.
2005-12-21
  FIX   Various fixes, new feature block, embryonic themeability for wotd block
2005-12-14
  FEAT  First usable version
